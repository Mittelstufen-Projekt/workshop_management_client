import { Palette } from "../Palette.slint";

export component LagerText inherits Text{

    in-out property <string> label;
        color: Palette.accent-color;
        font-size: Palette.font-size * 1.5px;
        text: label;
    }