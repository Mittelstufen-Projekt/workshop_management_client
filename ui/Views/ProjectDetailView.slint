import { ProjectDetailRow } from "../Widgets/ProjectDetailRow.slint";
import { VerticalBox, HorizontalBox, Button } from "std-widgets.slint";
import { TextField } from "../Widgets/TextField.slint";
import { ProjectModel } from "../models/ProjectModel.slint";
import { ProjectView } from "ProjectView.slint";



export component ProjectDetailView inherits VerticalLayout {
    in-out property <string> projectController <=> name.text;
    in-out property <string> clientController;
    in-out property <string> eMailController;
    in-out property <string> phoneController;
    in-out property <string> projectStartController;
    in-out property <string> deadlineController;
    in-out property <string> calcCostsController;
    in-out property <string> currentCostsController;

    // TODO(jmeusel): refactor
    in property <ProjectModel> project;
    ProjectDetailRow {
        width: 300px; //klappt....
        title: "project.name";
        name := TextField {

        }
        Button {
            width: 30px;
            text: "X";
            clicked => {}
        }
    }
    ProjectDetailRow {
        width: 300px; //klappt....
        title: "project.client";
        TextField {}
        Button {
            width: 30px;
            text: "X";
            clicked => {}
        }
    }
    HorizontalBox {
        padding: 0;
        ProjectDetailRow {
            width: 300px; //klappt....
            title: "project.Email";
            TextField {}
            Button {
                width: 30px;
                text: "X";
                clicked => {}
            }
        }
        ProjectDetailRow {
            width: 300px; //klappt....
            title: "project.Tel.:";
            TextField {}
            Button {
                width: 30px;
                text: "X";
                clicked => {}
            }
        }
    }
    ProjectDetailRow {
        width: 300px; //klappt....
        title: "project.Project-start";
        TextField {}
        Button {
            width: 30px;
            text: "X";
            clicked => {}
        }
    }
    ProjectDetailRow {
        width: 300px; //klappt....
        title: "project-deadline";
        TextField {}
        Button {
            width: 30px;
            text: "X";
            clicked => {}
        }
    }
    ProjectDetailRow {
        width: 300px; //klappt....
        title: "project.calculated costs";
        TextField {}
        Button {
            width: 30px;
            text: "X";
            clicked => {}
        }
    }
    ProjectDetailRow {
        width: 300px; //klappt....
        title: "project.currentcosts";
        TextField {

        }
        Button {
            width: 30px;
            text: "X";
            clicked => {}
        }
    }
}
