import { VerticalBox, HorizontalBox, StandardButton } from "std-widgets.slint";
import { TextField } from "../Widgets/TextField.slint";
import { Constants } from "../Constants.slint";
import { Button } from "../Widgets/Button.slint";


export component ClientPopUp inherits Rectangle {
    // TODO(jmeusel): Client hat nur email und name

    in-out property <bool> showPopUp;
    in-out property <string> nameController;
    in-out property <string> emailController;
    in-out property <string> phoneController;
    visible: showPopUp;


    /*
    DB model:
    pub struct Client {
    pub id: i32,
    pub firstname: String,
    pub lastname: String,
    pub phone: String,
}
    */
    HorizontalLayout {
        alignment: center;
        VerticalLayout {
            alignment: start;
            padding-top: 200px;
            Rectangle {
                background: lightgray;
                width: 400px;
                height: 260px;
                VerticalBox {
                    HorizontalBox {
                        Text {
                            text: Constants.name + ":";
                        }
        
                        TextField {
                            text: nameController;
                        }
                    }
        
                    HorizontalBox {
                        Text {
                            text: Constants.email + ":";
                        }
        
                        TextField {
                            text: emailController;
                        }
                    }
        
                    HorizontalBox {
                        Text {
                            text: Constants.phone + ":";
                        }
        
                        TextField {
                            text: phoneController;
                        }
                    }
        
                    HorizontalBox {
                        Button {
                            text: "Cancel";
                            clicked => {
                                // TODO(jmeusel): cancel
                        }
                        }
        
                        Button {
                            text: "Save";
                            clicked => {
                                // TODO(jmeusel): Save
                        }
                        }
                    }
                }
            }
        }
    }
}
